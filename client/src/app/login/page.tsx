import React from 'react';
import LoginForm from './Login';

export const metadata = {
  title: 'Login | Exam Portal',
  description: 'Portal Login Page',
};

const Login = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default Login;
